<%- include('../partials/header.ejs') %>

    <div class="container">

    <%- include('../partials/menu.ejs') %>

        <div class="card">
            <div class="card-header">LISTAGEM DE CLIENTES</div>
        </div>

    <%
        cliente.data.forEach(cliente=>{      
    %>
        <div class="card mt-2 p-1">
            <div class="card-body">
                <%= cliente.nome_Cliente %>
            </div>

            <div class="btn-group mr-2">
                <a href="/editarClientes/<%= cliente.id_Clientes %>" class="btn btn-success btn-sm mr-2 ml-2">EDITAR CLIENTE</a>
                <a href="#" class="btn btn-danger btn-sm mr-2 ml-2">EXCLUIR CLIENTE</a>
            </div>
        </div>

    <% }) %>

    </div>

</body>

<%- include('../partials/footer.ejs') %>

</html>